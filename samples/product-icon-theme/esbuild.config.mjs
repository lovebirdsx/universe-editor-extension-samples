/*
 * product-icon-theme ships no JavaScript — the product icon theme comes entirely
 * from package.json `contributes.productIconThemes`. This file exists so the e2e
 * runner's per-sample build step has a target to invoke.
 */
console.log('product-icon-theme: pure declarative, nothing to build')
