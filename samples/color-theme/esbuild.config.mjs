/*
 * color-theme ships no JavaScript — the color theme comes entirely from
 * package.json `contributes.themes`. This file exists so the e2e runner's
 * per-sample build step has a target to invoke.
 */
console.log('color-theme: pure declarative, nothing to build')
