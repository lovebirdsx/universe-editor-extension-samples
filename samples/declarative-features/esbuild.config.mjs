/*
 * declarative-features ships no JavaScript — every capability comes from
 * package.json `contributes` (jsonValidation / grammars / iconThemes). This file
 * exists so the e2e runner's per-sample build step has a target to invoke.
 */
console.log('declarative-features: pure declarative, nothing to build')
