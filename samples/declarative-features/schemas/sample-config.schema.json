{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The sample config name."
    },
    "version": {
      "type": "number",
      "description": "The numeric version of the sample config."
    }
  }
}
